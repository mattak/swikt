import {TArray, TObject} from "../../util/Tree.ts";
import {SwiftKotlinConverter} from "../SwiftKotlinConverter.ts";
import {createJumpReturnExpression} from "../util/expression.ts";

// export function convert_returnStatement__expression(self: SwiftKotlinConverter, path: string[], input: TArray): TObject {
//   return createJumpReturnExpression(expression);
// }